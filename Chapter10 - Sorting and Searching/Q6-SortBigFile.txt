Divide the file into chunks of X bytes, where X is the amount of memory we have available. We then sort each of those chunks and write back to the file system. Once all of them are sorted, they're are merged one by one, having a fully sorted file. This algorithm is called External Sort.
